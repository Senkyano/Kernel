[package]
name    = "kernel"
version = "0.1.0"
edition = "2021"

[lib]
name		= "kernel"
path		= "srcs/kernel.rs"
crate-type	= ["staticlib"]

[profile.release]
panic     = "abort"
opt-level = 2

[profile.dev]
panic = "abort"